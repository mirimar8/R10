import React from 'react';
import {
    ScrollView,
    View,
    Text,
} from 'react-native';

const Map = () => {
    return (
        <ScrollView>
            <View >
                <Text>Map is not available right now for android</Text>
            </View>
        </ScrollView >
    );
};

export default Map;
